import { Button } from '@/components/ui/Button';
import { Download } from 'lucide-react';

export function {ModelUpper}Header({ onCreate }: { onCreate: () => void }) {
  return (
    <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
      <div>
        <h2 className="text-2xl font-bold text-primary-800">{ModelVisible}s</h2>
        <p className="text-primary-500">Gerencie todos os seus {ModelVisible}s</p>
      </div>
      <div className="flex gap-2">
        <Button variant="secondary" onClick={() => window.print()}>
          <Download size={18} className="mr-2" /> Exportar
        </Button>
        <Button onClick={onCreate}>Novo {ModelVisible}</Button>
      </div>
    </div>
  );
}
